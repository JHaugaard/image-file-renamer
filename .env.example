# Image File Renamer - Environment Variables
# Copy this file to .env.local and configure as needed

# Note: This is a client-side only application
# No backend API keys or secrets are needed

# Development Port (optional, defaults to 5173)
# VITE_PORT=5173

# Add any future environment variables here
# All Vite env vars must start with VITE_ to be exposed to client
